
<div class = 'center'><img src= "<%= current_user.photo_url %>" alt= "profile image" height="100" width="100"></div>
<h1 class = 'center'>Your Feed</h1>

<div class = 'center'>Followers: <%=current_user.followers.count%> | Following: <%=current_user.followees.count%></div>
<br>
<ul>
<%current_user.feed.each do |post|%>
<li><%=link_to post.title, post_path(post)%> | by <%=link_to post.user.name, user_path(post.user)%> </li>
<div> Zens: <%=post.zens.count%> | Comments: <%=post.comments.count%></div>
<%end%>
</ul>

<%=button_to "Create a Post", new_post_path, method: 'get'%> 
